<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.athome.springbootwithmybatis.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.athome.springbootwithmybatis.entity.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="userName" />
        <result column="passWord" jdbcType="VARCHAR" property="passWord" />
        <result column="realName" jdbcType="VARCHAR" property="realName" />
    </resultMap>

    <insert id="register" parameterType="com.athome.springbootwithmybatis.entity.User">
        insert into user(userName, passWord) Values(#{userName}, #{passWord})
    </insert>
<!--    修改用户密码-->
    <update id="updateUser" parameterType="com.athome.springbootwithmybatis.entity.User">
        update user set `userName` = #{userName}, `passWord` = #{passWord} where id = #{id}
    </update>

    <select id="login" resultType="com.athome.springbootwithmybatis.entity.User">
        select * from user where userName = #{userName} and passWord = #{passWord}
    </select>

<!--    <select id="login" parameterType="com.athome.springbootwithmybatis.entity.User" resultType="com.athome.springbootwithmybatis.entity.User">-->
<!--        select * from user where userName = #{userName} and passWord = #{passWord}-->
<!--    </select>-->

</mapper>